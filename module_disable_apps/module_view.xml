<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- Disable default "Apps" filter on module list -->
  <record id="base.open_module_tree" model="ir.actions.act_window">
    <field name="context">{
      'search_default_app': False
    }</field>
  </record>

</odoo>
